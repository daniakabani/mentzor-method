import { trackerPageMetadata } from 'src/constants/metadata'
import { Tracker } from 'src/ui/pages'

export const metadata = trackerPageMetadata

export default function IntensityTrackerPage() {
  return <Tracker />
}
